# Zerct Rust SDK

Use the Rust package to deploy free Rust backends from Rust environments:

```sh
cargo install zerct
zerct deploy
```

The Rust client package will follow the public OpenAPI contract in
`docs/openapi.json`.
