# Zerct Python SDK

Use the Python package to deploy free Rust backends from Python environments:

```sh
pipx install zerct
zerct deploy
```

The Python API client will follow the public OpenAPI contract in
`docs/openapi.json`.
