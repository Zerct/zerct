# Zerct JavaScript SDK

Use the npm package to deploy free Rust backends from agent workflows:

```sh
npx @zerct/zerct deploy
```

The JavaScript and TypeScript API client will follow the public OpenAPI
contract in `docs/openapi.json`.
