---
title: "Tovuk"
description: "Free Rust worker cloud for agents."
---

Ship a Rust worker, static frontend, SQLite data, and an HTTPS endpoint from
one command.
Tovuk returns structured feedback so agents can fix issues and deploy again.

```sh
tovuk deploy
```

## Choose your path

<CardGroup cols={2}>
  <Card title="Deploy in minutes" icon="rocket" href="/quickstart">
    Create a `tovuk.toml`, run checks, and deploy a Rust worker from one
    command.
  </Card>
  <Card title="Give agents the loop" icon="bot" href="/agents">
    Copy the exact check, dry-run, deploy, log, and support workflow for coding
    agents.
  </Card>
  <Card title="Plan safe usage" icon="gauge" href="/pricing">
    Review free limits, Pro allowances, overage meters, and hard-cap commands.
  </Card>
  <Card title="Read the contract" icon="file-code" href="/reference/project-contract">
    Understand the project file, capabilities, workers, resources, and runtime
    conventions.
  </Card>
</CardGroup>

## Start

- Add a `tovuk.toml` project contract.
- Run `check` before `deploy`.
- Fix any JSON `agent_instruction`.
- Deploy when the checks pass.

```sh
tovuk check
tovuk deploy
```

## Agent prompt

```text
Use Tovuk to deploy this Rust worker for free.
Read https://docs.tovuk.com/llms.txt.
Run `tovuk check`.
Fix any JSON `agent_instruction`.
Run `tovuk deploy`.
```

## Packages

```sh
npm install -g tovuk
brew tap tovuk/tovuk https://github.com/tovuk/tovuk
brew install tovuk
pipx install tovuk
cargo install tovuk
tovuk deploy
```
