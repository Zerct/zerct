---
title: "Tovuk"
description: "Deploy Rust workers, static frontends, and full-stack services with agent-readable checks."
---

Ship Rust workers, static frontends, full-stack services, SQLite data, and
HTTPS endpoints from one command.
Tovuk returns structured feedback so agents can fix issues and deploy again.

```sh
tovuk deploy
```

## Choose your path

<CardGroup cols={2}>
  <Card title="Deploy Rust Workers and Full-Stack Services" icon="rocket" href="/quickstart">
    Create a project contract, run checks, and deploy a Rust worker or
    full-stack service.
  </Card>
  <Card title="Run Agent Deploys" icon="bot" href="/agents">
    Follow the check, dry-run, deploy, log, support, and abuse workflow for
    coding agents.
  </Card>
  <Card title="Create Projects from Templates" icon="copy-plus" href="/templates">
    Start Rust worker APIs, TanStack static frontends, or full-stack Rust
    services with `tovuk new`.
  </Card>
  <Card title="Deploy and Recover Services" icon="terminal" href="/deploy">
    Check, dry-run, deploy, inspect, and recover Rust workers, static frontends,
    and full-stack services.
  </Card>
  <Card title="Prepare for Production" icon="shield-check" href="/production-readiness">
    Check dry-run output, usage caps, runtime dependencies, deploy ids, logs,
    status, domains, and cleanup before launch.
  </Card>
  <Card title="Pricing and Usage Limits" icon="gauge" href="/pricing">
    Compare Free and Pro limits, overage meters, and hard-cap commands.
  </Card>
  <Card title="Configure Tovuk Projects" icon="file-code" href="/reference/project-contract">
    Set required files, capabilities, build commands, and runtime behavior.
  </Card>
</CardGroup>

## Start

- Add a `tovuk.toml` project contract.
- Run `tovuk check --json` before `tovuk deploy --dry-run --json`.
- Fix any JSON `agent_instruction`.
- Review dry-run output before `tovuk deploy --wait --json`.

```sh
tovuk check --json
tovuk deploy --dry-run --json
tovuk deploy --wait --json
```

## Agent prompt

```text
Use Tovuk to deploy this project.
Read https://docs.tovuk.com/llms.txt.
Run `tovuk check --json`.
Fix any JSON `agent_instruction`.
Run `tovuk deploy --dry-run --json`.
Run `tovuk deploy --wait --json`.
```

## Packages

```sh
npm install -g tovuk
brew tap tovuk/tovuk https://github.com/tovuk/tovuk
brew install tovuk
pipx install tovuk
cargo install tovuk
tovuk deploy
```
