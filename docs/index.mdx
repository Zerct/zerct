---
title: "Tovuk"
description: "Free Rust worker cloud for agents."
---

Ship a Rust worker, static frontend, SQLite data, and an HTTPS endpoint from
one command.
Tovuk returns structured feedback so agents can fix issues and deploy again.

```sh
tovuk deploy
```

## Start

- Add a `tovuk.toml` project contract.
- Run `doctor` before `deploy`.
- Fix any JSON `agent_instruction`.
- Deploy when the checks pass.

```sh
tovuk doctor
tovuk deploy
```

## Agent prompt

```text
Use Tovuk to deploy this Rust worker for free.
Read https://docs.tovuk.com/llms.txt.
Run `tovuk doctor`.
Fix any JSON `agent_instruction`.
Run `tovuk deploy`.
```

## Packages

```sh
tovuk deploy
brew tap tovuk/tovuk https://github.com/tovuk/tovuk
brew install tovuk
pipx install tovuk
cargo install tovuk
```
